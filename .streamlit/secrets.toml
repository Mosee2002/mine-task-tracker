# =====================================================================
#  ⚠️⚠️⚠️ BEFORE YOU PASTE THIS INTO STREAMLIT CLOUD ⚠️⚠️⚠️
#
#  OWNER_USERNAME below is still a placeholder. You already have a
#  working owner account on your live app — I have no way to see what
#  username you actually set for it, since you configured that
#  directly in the Cloud Secrets panel, not through me. If you paste
#  this file as-is, you will LOSE OWNER CONSOLE ACCESS again.
#
#  -> Replace OWNER_USERNAME below with your real, current owner
#     username before saving.
#  -> Also replace APP_URL with your actual Streamlit Cloud app URL
#     (something like https://your-app-name.streamlit.app) — it's
#     still set to localhost here, which breaks password-reset links.
# =====================================================================

#  Save this file as:   .streamlit/secrets.toml
#  (relative to wherever you run `streamlit run app.py`)
#
#  ⚠️  ROTATE THE KEY BELOW if you haven't already. It is byte-for-byte
#      the same key that was hardcoded in your original app.py, and it
#      has appeared in source files and chat logs. Supabase Dashboard →
#      Project Settings → API → rotate the anon key, then replace the
#      SUPABASE_KEY line here. If you already rotated it, use YOUR new
#      key instead of the one below, not this one.
#
#  ⚠️  Add this to .gitignore BEFORE saving it (not relevant if you're
#      pasting into Streamlit Cloud's Secrets panel rather than a file):
#          echo ".streamlit/secrets.toml" >> .gitignore
# =====================================================================

# ---------- REQUIRED ----------
SUPABASE_URL = "https://xvfbxogzefhmitrtykce.supabase.co"
SUPABASE_KEY = "sb_publishable_5PwU6p2yXq-R7zuMN_7E2A_e-iUv8vi"

# ---------- OWNER (access control) ----------
# The username that gets the Owner Console: approving who may use the
# app, assigning roles, suspending accounts.
#
# This is read from HERE, not from the database, on purpose. If "owner"
# were a database role, anyone who could write that table — directly
# with the anon key, or through a bug in a role-editing screen — could
# make themselves owner. Anchoring it to secrets.toml means taking
# ownership requires access to the deployment config.
#
# Set this to the username you create in the first-run setup form.
OWNER_USERNAME = "Admin Moses"

# ---------- RECOMMENDED ----------
# Public URL of the deployed app. Password-reset emails link here, so a
# wrong value produces reset links that go nowhere.
APP_URL = "https://mine-task-tracker-ljc2ppwmwiwtfqwykn2by5.streamlit.app"

# Salt for private-chat key derivation. Freshly generated for you.
# NOTE: this does NOT make private chat end-to-end encrypted — see the
# warning shown in the app itself.
CHAT_KEY_SALT = "0jUCRDs_uEsWXmxOsFJYeCGScJccBRILWw9T-WsfAQy4y3bHlqNJDqC2Gsx34had"

# Minutes of inactivity before logout. Lower this for shared terminals.
SESSION_TIMEOUT_MINUTES = 60

# Per-file upload cap, in MB.
MAX_UPLOAD_SIZE_MB = 5

SUPABASE_SERVICE_ROLE_KEY = "sb_secret_3_mEJ3rjA0uMpQwerEASCQ_JpTp6--Z"

# ---------- OPTIONAL: email notifications ----------
# Leave commented out and the app silently skips email (no errors).
# For Gmail/Workspace, SMTP_PASSWORD must be an App Password.
# SMTP_SERVER   = "smtp.gmail.com"
# SMTP_PORT     = 587
# SMTP_USER     = "maintenance-noreply@yourcompany.com"
# SMTP_PASSWORD = "your-app-specific-password"
# SMTP_FROM     = "maintenance-noreply@yourcompany.com"

# ---------- OPTIONAL: Slack / Teams alerts ----------
# High/Critical incidents and safety-flagged handovers post here.
# SLACK_WEBHOOK = "https://hooks.slack.com/services/..."
# TEAMS_WEBHOOK = "https://yourorg.webhook.office.com/..."

# ---------- OPTIONAL: Google OAuth ----------
# Leave blank. The login flow is still a stub in the code; setting these
# only shows a button that does not complete a sign-in.
# GOOGLE_CLIENT_ID     = ""
# GOOGLE_CLIENT_SECRET = ""
# GOOGLE_REDIRECT_URI  = ""
VAPID_PUBLIC_KEY = "BGoPb9E6sE0HhLzPurq_dMCo4tsPbOi3CYm_51p5bnJ8zUNFqYkLX7QCsebR0_67AI857cgXYHDafPotivy41uk"
VAPID_PRIVATE_KEY = "MIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQgnFl2Vy8WwpOw6Eo1W6HnKgLLyr3xRK4LCl5AivMi7EqhRANCAARqD2/ROrBNB4S8z7q6v3TAqOLbD2zotwmJv+daeW5yfM1DRamJC1+0ArHm0dP+uwCPOe3IF2Bw2nz6LYr8uNbp"
VAPID_CLAIMS_EMAIL = "anabamoses2002@gmail.com"

# ---------- OPTIONAL: Google Workspace mailbox auto-provisioning ----------
# Uncomment once you've completed GOOGLE_WORKSPACE_SETUP.md. Until then,
# access requests without email fall back to admin password reset.
# GOOGLE_WORKSPACE_SA_JSON = '{"type": "service_account", ...}'
# GOOGLE_WORKSPACE_ADMIN_EMAIL = "admin@gmc.com"
# GOOGLE_WORKSPACE_DOMAIN = "gmc.com"
